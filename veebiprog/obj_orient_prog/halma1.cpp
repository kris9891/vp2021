#include <iostream>
using namespace std;
int N, M, tr, tv
char **laud;



void tryki(char** layd){
	for(int rida = 0; rida<N; rida++){
		for(int veerg = 0; veerg<M; veerg++){
			cout << laud[rida][veerg];
		}
	}
}

void kuvalaud(char laud[][], **int N, int M)
	for(int rida=0; rida<N; rida++){
		for(int veerg=0; veerg<M; veerg++){
			cout << laud[rida][veerg]{
				cin >> laud[rida][veerg];
				if(laud[rida][veerg]=="#"){tr=rida; tv=veerg;}
			}
		}
		cout << laud[2][2] << << " " << tr << " " << tv << endl;
		for(int veerg=0; veerg<M, veerg++){
			cout << laud[rida][veerg];
		}
		cout << endl;
	}

int main(void){
	int N, M;
	cin >> N >> M;
	char laud[N][M];
	for(int rida=0; rida<N; rida++){
		for(int veerg=0; veerg<M, veerg++){
			cin >> laud[rida][veerg];
			if(laud[rida][veerg]=='#'){tr=rida; tv=veerg;}
		}
	}
	cout << laud[2][2] << " " << tr << 
}